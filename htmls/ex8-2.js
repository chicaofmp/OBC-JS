let spaceshipName = prompt("Nave: ")

let newSpaceshipName = ""

let forbiddenChar = prompt("Letra Para parar o processo: ")

for (spaceshipName.length = -1;let i >= 0 ; i--) {

    spaceshipName[i]
    console.log(spaceshipName)
    

}